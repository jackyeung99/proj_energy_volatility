storage:
  backend: s3
  bucket: volatility-project
  prefix: energy-vol-forecast
  region: us-east-2

steps:
  ingestion:
    enabled: true
    config: steps/ingest.yaml

  features:
    enabled: true
    config: steps/build_features.yaml

  merge:
    enabled: true
    config: steps/merge.yaml

  regime:
    enabled: true 
    config: steps/regime.yaml
    
  prediction:
    enabled: true
    config: steps/prediction.yaml

  scoring:
    enabled: true
    config: steps/scoring.yaml